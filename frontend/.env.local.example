# frontend/.env.local — never commit this file

# FastAPI backend base URL (HTTP)
# Jika dijalankan via Docker (frontend container berkomunikasi dengan backend via network docker internal atau proxy):
NEXT_PUBLIC_API_URL=http://localhost:8000
